use std::time::Duration;

pub const BOARD_WIDTH: usize = 10;
pub const BOARD_HEIGHT: usize = 20;
pub const FRAME_TIME: Duration = Duration::from_millis(16);
pub const FALL_INTERVAL: Duration = Duration::from_millis(350);
